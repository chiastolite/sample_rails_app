box: wercker/rvm
build:
  steps:
    - rvm-use:
        version: ruby-2.1.2
    - bundle-install
    - script:
        name: rake
        code: bundle exec rake
deploy:
  steps:
    - 1syo/idobata-notify@0.1.2:
        token: $IDOBATA_TOKEN
